<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/poopyBrown</item>
        <item name="colorPrimaryDark">@color/poopyBrown</item>
        <item name="colorAccent">@color/poopyBrown</item>
    </style>

</resources>
